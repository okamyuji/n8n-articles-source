name: gitleaks

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read
  pull-requests: read

jobs:
  # gitleaks による秘密情報スキャン（fetch-depth: 0 で履歴全体、GITHUB_TOKEN 内蔵）。
  # .gitleaks.toml はリポジトリ直下にあるため gitleaks が自動検出する。
  security:
    uses: okamyuji/reusable-workflows/.github/workflows/security-scan.yml@v1
